using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class GameState
{
    public static Vector2 spawnPoint;        //当前出生点（current checkpoint）s
    public static bool hasSpawnPoint;       //是否已经设置过Checkpoint（重生点）
}
